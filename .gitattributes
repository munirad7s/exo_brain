# Normalize line endings across machines (agents run on Windows, macOS, Linux).
* text=auto
*.md text
*.yaml text
*.sh text eol=lf

# If you store large binaries in the vault, install git-lfs and uncomment:
# *.mp4 filter=lfs diff=lfs merge=lfs -text
# *.zip filter=lfs diff=lfs merge=lfs -text
# *.pptx filter=lfs diff=lfs merge=lfs -text
# *.docx filter=lfs diff=lfs merge=lfs -text
